package com.patrickceledio;

// Import everything -- array, iterators

import java.util.*;
import java.util.stream.IntStream;

public class HelloWorld {

    // Receive input from user
    static Scanner sc = new Scanner(System.in);

    // final -- Cannot be changed
    // double -- store decimal numbers
    final double SHORTPI = 3.14159;

    // Execution starts inside main function by default
    public static void main(String[] args){
        // public -- anything that has access to class HelloWorld can run this
        // static -- function that belongs to the class
        // void -- function does not return anything after execution
        // String[] -- if we run our program on cmd, it will return a string array stored inside args


        // Print out to command prompt
        System.out.println("Hello World");

        // int -- store integer number that has no decimal places
        int var1 = 100;
        int v2, v3;

    }
}
